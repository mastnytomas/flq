{
	"version": 2,
	"builds": [
		{
			"src": "wsgi.py",
			"use": "@vercel/python"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "wsgi.py"
		}
	],
	"env": {
		"FLASK_ENV": "production",
		"DATABASE_URL": "@DATABASE_URL",
		"SOCCERDATA_DIR": "/tmp/soccerdata"
	}
}